{
  "name": "atlas-mcp-server-example",
  "version": "0.1.0",
  "private": true,
  "description": "Reference MCP server template for AtlasAgents — copy this to build your own MCP integration.",
  "type": "module",
  "main": "dist/stdio.js",
  "bin": {
    "atlas-mcp-server-example": "dist/stdio.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "start": "node dist/stdio.js",
    "test": "vitest run"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "typescript": "^6.0.3",
    "vitest": "^2.0.0"
  }
}
